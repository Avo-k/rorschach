# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/

# Virtualenv / uv
.venv/
.python-version

# Vestigial Maia-2 weights — safe to `rm -rf` locally.
maia2_models/

# Patricia binary (download via scripts; user can also vendor it)
# Comment this out if you want to track the binary in-repo.
bin/patricia
bin/patricia_v*

# Bench data (PGNs from Lichess API, extracted positions, results)
data/

# Local secrets (LICHESS_TOKEN, etc.)
.env
.env.local

# IDE
.vscode/
.idea/
*.swp
